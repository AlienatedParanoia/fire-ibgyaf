import { LandingExperience } from 'fire-platform';

// Full landing page — self-contained, no props or data fetching.
// Wide/tall editorial layout: recommend cardMode "column" for review.
export function Default() {
  return <LandingExperience />;
}
